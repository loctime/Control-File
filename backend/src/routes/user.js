const express = require('express');
const router = express.Router();

// Endpoints deprecated - taskbar now uses real folders with metadata.source === 'taskbar'
// This file is kept for compatibility but endpoints are no longer used

module.exports = router;


