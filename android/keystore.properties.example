# Archivo de ejemplo para configuración de firma digital
# Copiar a keystore.properties y configurar con valores reales

storeFile=keystore.jks
storePassword=tu_store_password
keyAlias=tu_key_alias
keyPassword=tu_key_password

# Instrucciones:
# 1. Generar keystore:
#    keytool -genkey -v -keystore keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias tu_alias
# 
# 2. Colocar keystore.jks en android/app/
# 
# 3. Configurar este archivo con las credenciales reales
# 
# 4. Para CI/CD, usar variables de entorno:
#    KEYSTORE_PASSWORD, KEY_ALIAS, KEY_PASSWORD
